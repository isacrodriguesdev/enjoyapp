import React from 'react';
import { View, StyleSheet } from 'react-native';
// imports
// types
// components

type Props = {};

function ModalFilter(props: Props) {

   return (
      <View style={styles.container}>

      </View>
   );
}

export default ModalFilter; 

const styles = StyleSheet.create({
   container: {}
});
